{
  "name": "accessmate",
  "version": "0.4.0",
  "private": true,
  "description": "AccessMate — three native apps (web, iOS, Android) for accessibility-first UK rail journeys.",
  "scripts": {
    "web": "pnpm --filter @accessmate/web dev",
    "web:build": "pnpm --filter @accessmate/web build",
    "web:typecheck": "pnpm --filter @accessmate/web typecheck"
  },
  "packageManager": "pnpm@9.0.0"
}
